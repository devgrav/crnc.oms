# Default Docker Compose profile set. Without this, adding `profiles:` to every
# service in docker-compose.yml would mean a bare `docker-compose up` starts nothing.
# `docker-compose --profile <name> up` (e.g. --profile security) overrides this default
# entirely rather than adding to it, so it still narrows the run to just that context.
COMPOSE_PROFILES=full
